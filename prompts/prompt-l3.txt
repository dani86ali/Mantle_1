Fix: The BullMQ pipeline queue infrastructure was added but the intake route still calls void runAndPersistPipeline directly. The switch to use the queue was never flipped. Step 1: Read src/app/api/intake/route.ts and find where void runAndPersistPipeline is called. Read src/lib/queue/pipeline-job.ts for the enqueuePipelineJob function or the pipelineQueue export. Step 2: Replace the void runAndPersistPipeline call with a conditional. Read process.env.INTAKE_INLINE. If it equals '1', keep the current inline behavior (void runAndPersistPipeline). Otherwise, try to enqueue via the pipeline queue. Wrap the enqueue call in a try-catch. If the enqueue throws (Redis not running), log a console.warn saying 'Pipeline queue unavailable, falling back to inline execution' and fall back to void runAndPersistPipeline. This way the app works with or without Redis. Step 3: When queued successfully, return status 202 instead of the current status. Add queued: true to the response JSON body. When inline (either by INTAKE_INLINE=1 or Redis fallback), keep the current response status and add queued: false. Step 4: Add INTAKE_INLINE=1 to .env if it exists. Add a comment line above it: Set to 1 to run pipeline inline without Redis. Remove or unset for queued execution via BullMQ. Step 5: Run npx tsc --noEmit and npx vitest run tests/api/ to verify. Show output.
