Fix: src/engines/e5/orchestrator-types.ts has requirementsBaseline typed as any which defeats type safety at the E4 to E5 boundary. Step 1: Read src/engines/e5/orchestrator-types.ts and find the requirementsBaseline field. Read src/engines/e4/preliminary-baseline.ts to see the shape that buildPreliminaryBaseline produces (5 categories: business, functional, nonFunctional, constraints, assumptions, each an array of objects). Also read src/engines/e4/requirements-baseline-builder.ts to see the phase2 full baseline shape. The Zod schema must accommodate BOTH the preliminary and full baseline structures. Read src/coordinator/pipeline-e5.ts parseBaseline to see how E5 currently consumes it. Step 2: Create a Zod schema in src/engines/e5/orchestrator-types.ts for the baseline shape. Define a BaselineEntry schema with id string, text string, priority string, and source optional string. Allow additional optional fields that phase2 may add (like category, gapType, confidence) using passthrough or explicit optional fields. The top-level schema has 5 optional category keys (business, functional, nonFunctional, constraints, assumptions), each an optional array of BaselineEntry. Export the schema and the inferred type. Step 3: Replace the any type on requirementsBaseline with the new Zod-inferred type. Step 4: In pipeline-e5.ts parseBaseline, add Zod safeParse validation after JSON.parse. If validation fails, log a warning with the Zod error issues and return empty categories. Do not throw. Step 5: Run npx vitest run tests/engines/e5/ tests/coordinator/ and npx tsc --noEmit. Show output.
