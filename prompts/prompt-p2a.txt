Fix: BOMatic upload relies on a heuristic file classifier that misclassifies XLSX uploads without "BOQ" in the filename, and selectBoQFilePath silently picks the first match among multiple XLSX uploads. Replace this with explicit user-assigned document types carried per uploaded file. This prompt covers the BACKEND only — UI redesign is a separate prompt. Step 1: Read src/lib/db/schema.ts and confirm the intakes table stores requirementsJson as a jsonb column — no Drizzle migration is needed because uploadedFiles lives inside this JSON blob. Read src/lib/middleware/validate.ts to find intakeFormSchema and the uploadedFiles field shape. Read src/app/api/upload/route.ts and src/app/api/intake/route.ts to see the current upload+intake flow. Read src/engines/e1/orchestrator-types.ts for E1InputFile, and src/coordinator/intake-file-loader.ts for UploadedFile and LoadedFile interfaces. Step 2: Define the document-type enum. Create src/types/document-type.ts exporting `export const DOCUMENT_TYPES = ['boq', 'rfp_sow', 'compliance', 'vendor_bom', 'prior_design', 'other'] as const; export type DocumentType = typeof DOCUMENT_TYPES[number];` and a Zod enum `documentTypeSchema = z.enum(DOCUMENT_TYPES)`. Step 3: Update /api/upload to accept a parallel-indexed types[] field. The form submitted to /api/upload now includes a `types` field as a JSON-encoded array of DocumentType. In src/app/api/upload/route.ts after extracting files (line 45), read `form.get("types")`, JSON.parse and validate it via z.array(documentTypeSchema). If missing or length-mismatched with files, default every entry to 'other' and add a warning to the response payload. Add documentType to UploadedFileMeta and return it. Step 4: Update intakeFormSchema's uploadedFiles entry in src/lib/middleware/validate.ts to include `documentType: documentTypeSchema`. Step 5: Update src/coordinator/intake-file-loader.ts UploadedFile and LoadedFile interfaces to carry documentType through; thread it into E1InputFile in src/engines/e1/orchestrator-types.ts and from buildPipelineInputForIntake (src/coordinator/intake-to-pipeline-input.ts) into the PipelineInput files array. Step 6: Update src/engines/e1/orchestrator-helpers.ts classifyAll: when an input file has an explicit documentType other than 'other', skip the 3-stage classifier and emit a classification with confidence 1.0 and stage 1, mapping documentType to existing type/subtype values: 'boq' -> { type:'commercial', subtype:'boq_template' }, 'rfp_sow' -> { type:'technical', subtype:'requirements' }, 'compliance' -> { type:'compliance', subtype:'general' }, 'vendor_bom' -> { type:'commercial', subtype:'vendor_bom' }, 'prior_design' -> { type:'technical', subtype:'engineering_drawing' }. For 'other', run the existing heuristic classifyFile. Step 7: Update src/coordinator/pipeline-e2.ts selectBoQFilePath: when the input has any file with documentType='boq', return that file's path directly (highest priority). Fall back to the existing subtype heuristic only when no explicit boq is set. The signature stays the same (it reads from E1Output.fileClassifications, which now carries the explicit-type mapping through). Step 8: Write vitest tests. tests/coordinator/pipeline-e2.test.ts: explicit boq documentType beats heuristic; multiple files with one boq-typed and one unknown XLSX — only the boq-typed path is returned. tests/engines/e1/file-classifier.test.ts: classifyAll with documentType='rfp_sow' emits a requirements classification at confidence 1.0 without running heuristic stages; documentType='other' still runs heuristics. tests/app/api/upload.test.ts (create if missing): /api/upload accepts a types array and returns UploadedFileMeta entries with documentType set. Step 9: Run `npx vitest run tests/` and `npx tsc --noEmit`. Show output. Constraints: NO Drizzle migration — documentType piggybacks on existing jsonb. File-size budget 200 LOC per changed file. Public types must be exported and typed.
