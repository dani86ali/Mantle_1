Fix: E1's requirements extractor, risk flagger, deadline extractor, and missing-doc detector are blind to XLSX content because src/coordinator/intake-file-loader.ts line 19 EXTRACTABLE = new Set([".pdf", ".docx", ".doc"]) — XLSX files reach E1 with content unset. src/engines/e1/orchestrator-helpers.ts buildTextMap (lines 41-47) only adds files where f.content is non-empty. So even a correctly-classified BoQ XLSX has zero rows visible to extractAllRequirements, flagLegalTraps, extractDeadlines, detectMissingDocuments. Step 1: Read src/coordinator/intake-file-loader.ts and src/lib/io/excel-reader.ts to understand the existing readExcelFile helper — it returns sheetNames and a sheets record where each sheet is string[][]. Step 2: Extend enrichFileContent in intake-file-loader.ts to handle .xlsx, .xls, .csv. For XLSX/XLS, call readExcelFile and flatten every non-empty cell into a single newline-delimited text blob; prefix each sheet with `--- Sheet: <name> ---` so multi-sheet workbooks remain distinguishable; assign the result to LoadedFile.content. Wrap in try/catch and push a warning on failure (do not throw). For CSV, read the file as UTF-8 text directly via fs/promises readFile. Step 3: Do NOT modify buildTextMap in src/engines/e1/orchestrator-helpers.ts — its existing logic accepts any non-empty content. Do NOT modify loadExcelSheets — it still needs to load the parsed sheet structure for extractVendors and analyzeEvalCriteria separately from textMap. Step 4: Update src/engines/e1/file-classifier.ts scanContentKeywords behavior for spreadsheets. The XLSX content now reaches stage 3, so the spreadsheet fallback at lines 162-171 may be partly subsumed by content-keyword matching when BoQ rows contain "unit price", "qty", "SAR", etc. Keep the spreadsheet fallback in place as a safety net but raise its confidence from 0.4 to 0.5 to reflect that XLSX content is now meaningfully scannable; leave the existing reorder unchanged so the fallback still fires when scanContentKeywords yields unknown. Step 5: Add vitest tests. Test 1 in tests/coordinator/intake-file-loader.test.ts (create if missing): enrichFileContent on a fixture XLSX with two cells "shall comply" and "unit price" returns a LoadedFile whose content contains both phrases. Test 2 in tests/engines/e1/orchestrator.test.ts or a new file: runE1 on an input with only an XLSX file produces at least one requirement (proves the requirements extractor saw the XLSX content). Use a minimal fixture XLSX written via exceljs in the test setup if no fixture exists. Step 6: Run `npx vitest run tests/engines/e1/ tests/coordinator/` and `npx tsc --noEmit`. Show full output. Constraints: do not add new runtime dependencies; reuse existing readExcelFile. Function signatures must remain typed (no any).
