Fix: fillFromPriced only works for Type A (Ariba) BoQ templates. Types B (NRM2), C (Vendor Quote), D (BoM), and E (Telecom) silently return undefined with no filled template and no warning.

Step 1: Read these files to understand the Type A pattern and each type's column layout:
Read src/engines/e2/fill-from-priced.ts which returns undefined for non-Type-A at line 35.
Read src/engines/e2/boq-template-filler.ts to see how Type A writeback works with ExcelJS.
Read src/engines/e2/parsers/type-b-nrm2.ts and find where it reads columns (which columns hold unit price, quantity, description).
Read src/engines/e2/parsers/type-c-vendor-quote.ts for Type C column positions.
Read src/engines/e2/parsers/type-d-bom.ts for Type D column positions.
Read src/engines/e2/parsers/type-e-telecom.ts for Type E column positions.
Read src/engines/e2/boq-types.ts for the BoQType enum and BoQLineItem type.

Step 2: Create per-type writeback modules under src/engines/e2/writers/.
For each type, create a writer that uses ExcelJS (not xlsx/SheetJS) to preserve formatting:

src/engines/e2/writers/write-type-b.ts: Read the source workbook with ExcelJS. Find the price columns using Type B parser's column knowledge. For each matched row write unitPrice and extendedPrice (unitPrice times quantity). Save to output path. Export writeTypeB(sourceFilePath, pricedLines, outputPath): Promise<string>.

src/engines/e2/writers/write-type-c.ts: Same pattern for Type C columns.
src/engines/e2/writers/write-type-d.ts: Same pattern for Type D columns.
src/engines/e2/writers/write-type-e.ts: Same pattern for Type E columns.

Each writer should: use ExcelJS to preserve formatting and styles and merged cells; match priced lines to source rows by partNumber or itemNumber or row index; write unit price and extended price into the correct columns; handle missing matches gracefully by skipping the row; return the output file path; stay under 200 lines.

Step 3: Update fill-from-priced.ts to be a dispatcher.
Remove the early return for non-Type-A at line 35. Instead switch on type:
TYPE_A_ARIBA uses the existing fillBoQTemplate path unchanged.
TYPE_B_NRM2 calls writeTypeB.
TYPE_C_VENDOR_QUOTE calls writeTypeC.
TYPE_D_BOM calls writeTypeD.
TYPE_E_TELECOM calls writeTypeE.
TYPE_UNKNOWN calls a generic fallback.

Step 4: Add a generic fallback for TYPE_UNKNOWN.
For unrecognized formats, generate a standalone priced workbook using the existing bom-workbook-writer.ts. Return the path and add a console.warn: "Client template type not recognized. Generic priced sheet provided instead."

Constraints: Do NOT modify the parsers. Do NOT modify boq-template-filler.ts. Each new writer under 200 lines. Match between priced lines and source rows by partNumber first, falling back to row index.
Run: npx vitest run tests/engines/e2/ 2>&1 | Select-Object -Last 20
Run: npx tsc --noEmit 2>&1 | Select-Object -Last 10
Show me test and tsc output.
