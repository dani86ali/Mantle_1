The fuzzy SKU matcher at src/engines/e2/fuzzy-sku-matcher.ts exists and is tested but the E2 orchestrator skips it with a comment saying no catalog source. Step 1: Read src/engines/e2/fuzzy-sku-matcher.ts to understand its interface and what input it needs. Read src/engines/e2/orchestrator.ts and find the skipped call around line 111. Step 2: The catalog adapter is now wired via pipeline-e2-pricing.ts which provides listPrices as a Record of sku to number. The fuzzy matcher can use the keys of listPrices as the known SKU list to match against. Wire the fuzzy matcher into the orchestrator by passing Object.keys(input.listPrices || {}) as the known SKU list. If listPrices is empty or undefined, skip the matcher as before with a log message. Step 3: Remove the skip comment. The matcher should run after device expansion and before pricing, so typos like C9300-24P get corrected to C9300-24P-A before the price lookup. Step 4: Run npx vitest run tests/engines/e2/ and npx tsc --noEmit. Show output.
