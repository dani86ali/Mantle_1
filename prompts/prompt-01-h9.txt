Fix: Production adapter code dynamically imports from tests/mocks/ at runtime, creating a dependency on test fixtures in production builds.

Step 1: Find all dynamic imports of test fixtures from production code.
Run: grep -rn "tests/mocks" src/ --include="*.ts" --include="*.tsx"
Read every file that has a match.

Step 2: For each file with a tests/mocks import, extract the mock data functions into a co-located file in the same directory. For example:
If src/lib/adapters/catalog.ts imports from tests/mocks/index, create src/lib/adapters/_catalog-mock-data.ts with the mock catalog data.
If src/lib/adapters/estimate.ts imports from tests/mocks, create src/lib/adapters/_estimate-mock-data.ts.
Check src/lib/adapters/customer.ts for the same pattern.

Move ONLY the data and factory functions, not test assertions. Keep the isMockMode() gating exactly as-is. Change the import path from tests/mocks/... to the new co-located file.

Step 3: Verify no production code imports from tests/ anymore.
Run: grep -rn "tests/" src/ --include="*.ts" --include="*.tsx" | grep -v "\.test\." | grep -v "\.spec\."
Result should be empty.

Step 4: Verify test files that import from tests/mocks/ still work. Do NOT change test imports.

Constraints: Do NOT delete tests/mocks/ or change any test file imports. Do NOT change mock behavior or isMockMode() gating. Keep new files under 200 lines each. If data is large, split into multiple mock files.
Run: npx vitest run 2>&1 | Select-Object -Last 15
Run: npx tsc --noEmit 2>&1 | Select-Object -Last 10
Show me the grep result confirming no tests/ imports remain in src/, test output, and tsc output.
