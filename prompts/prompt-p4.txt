Fix: src/coordinator/router.ts line 4 SEQUENCES.rfp = ['e1', 'e2', 'e3'] omits E5, so RFP bids never generate HLD/LLD design artifacts. Add E5 to the RFP sequence and adapt buildE5Input to work without an E4 questionnaire baseline. The HLD/LLD document rendering itself (T6d/T6e per project memory) remains out of scope — this prompt only wires E5 into the RFP sequence so it produces its componentList artifact. Step 1: Read src/coordinator/router.ts to see the current SEQUENCES table. Read src/coordinator/pipeline-e5.ts buildE5Input and toE5Artifacts to identify exactly which fields buildE5Input pulls from state.artifacts.e4 (the RFI questionnaire baseline). Read src/engines/e5/orchestrator.ts entry signature and src/engines/e5/types.ts for E5Input. Step 2: Decide the RFP-mode E5 input shape. RFP mode has E1 output (requirements, eval criteria, sector, frameworks) but no E4 questionnaire baseline. Add an exported helper synthesizeE5InputFromE1(e1: E1Output, input: PipelineInput, outputDir: string): E5Input in src/coordinator/pipeline-e5.ts that maps E1 requirements (filtered to those classified 'mandatory' plus any with a technical indicator) into the requirementsBaseline categories E5 expects. Use the Zod schema introduced for requirementsBaseline (see project memory on M1 fix) — split requirements heuristically into business/functional/nonFunctional/constraints/assumptions buckets using simple keyword rules (default to functional for ambiguous entries). Step 3: Update src/coordinator/pipeline-engine-dispatcher.ts runE5 path (lines 45-50) so that when state.artifacts.e4 is absent (RFP mode), it calls synthesizeE5InputFromE1 with out.e1Output instead of buildE5Input. When state.artifacts.e4 is present (RFI mode), keep the current buildE5Input path. Step 4: Update src/coordinator/router.ts SEQUENCES.rfp from ['e1','e2','e3'] to ['e1','e5','e2','e3']. The order matters: E5 must run after E1 (needs requirements) and before E2 (E2 may consume E5's componentList via resolveE2Devices when no BoQ file is uploaded). Confirm in src/coordinator/pipeline-e2.ts that resolveE2Devices already prefers explicit devices over E5 component list — it does (read line 78-87) — so RFP mode with an uploaded BoQ file still uses the BoQ; E5 components are a fallback. Step 5: Write vitest tests. tests/coordinator/router.test.ts: getEngineSequence('rfp') returns ['e1','e5','e2','e3']; getEngineSequence('rfi') unchanged. tests/coordinator/pipeline-e5.test.ts: synthesizeE5InputFromE1 produces a valid E5Input from a fixture E1Output with 3 mandatory requirements; categories sum to the input count (no requirements dropped); requirementsBaseline passes the Zod schema. Step 6: Run `npx vitest run tests/coordinator/` and `npx tsc --noEmit`. Show output. Constraints: do not modify src/engines/e5/ internals — only coordinator/router/pipeline-e5 changes. The HLD/LLD document generation is OUT OF SCOPE — this prompt only wires E5 into the sequence and produces the component-list artifact. File-size budget 200 LOC per coordinator file.
