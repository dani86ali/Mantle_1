Fix: Replace the single drag-drop file picker on the RFP estimate-new form with six labeled drop zones, one per document type, each enforcing per-slot file-type validation and required-slot gating. Requires P2a backend work to be merged first (documentType plumbed end-to-end). Step 1: Read src/app/estimate/new/page.tsx to locate the current upload UI for RFP mode. Find the file input element, the state holding uploadedFiles, the form-submit handler, and the path that POSTs to /api/upload before /api/intake. Identify the existing Tailwind/component patterns used for inputs and form controls so the new slot UI matches. Step 2: Define the six slots in a new module src/components/intake/document-slots.ts exporting an array typed against DocumentType from src/types/document-type.ts: [{ id:'boq', label:'Bill of Quantities', required:true, multi:false, accept:['.xlsx','.xls','.csv'] }, { id:'rfp_sow', label:'RFP / SoW Document', required:true, multi:false, accept:['.pdf','.docx','.doc'] }, { id:'compliance', label:'Compliance Template', required:false, multi:false, accept:['.pdf','.docx','.xlsx'] }, { id:'vendor_bom', label:'Vendor BoMs', required:false, multi:true, accept:['.xlsx','.xls','.pdf','.docx'] }, { id:'prior_design', label:'Prior HLD/LLD', required:false, multi:true, accept:['.pdf','.docx'] }, { id:'other', label:'Other / References', required:false, multi:true, accept:['.pdf','.docx','.xlsx','.xls','.csv','.msg','.dwg'] }]. Step 3: Create a TypedSlotUpload component at src/components/intake/typed-slot-upload.tsx that renders one drop zone per slot. Each zone: drag-drop or click-to-select; rejection toast/inline-error on extension mismatch with the slot's accept list; per-slot file list with remove-X button; visual required indicator (asterisk + red border when empty AND required); single-file slots replace on second drop; multi-file slots append. The component exposes an onChange callback giving the parent the current { [slotId: DocumentType]: File[] } map and an onValidityChange callback indicating whether all required slots are populated. Keep under 400 LOC. Use existing Tailwind classes — do not import a new component library. Step 4: In src/app/estimate/new/page.tsx replace the existing upload UI for RFP mode only (RFI/RFI mode keeps its current single-input behavior). Wire the TypedSlotUpload into form state. When submit is clicked: validate both required slots ('boq' and 'rfp_sow') have at least one file each; if not, show inline error and prevent submit. Otherwise POST to /api/upload as multipart with files[] AND a parallel-indexed types[] JSON-stringified array of DocumentType values matching each file's slot. After success, use the returned UploadedFileMeta[] (now including documentType) as the intake form's uploadedFiles. Step 5: Wire the submit-button disabled state to the onValidityChange callback so the button is disabled until both required slots are populated. Step 6: Write a vitest+jsdom test in tests/components/typed-slot-upload.test.tsx using @testing-library/react. Tests: rendering shows six labeled zones; uploading an unsupported extension to the BoQ slot shows a rejection message; required-slot indicator renders for boq and rfp_sow only; multi-slot accepts multiple files; remove-X removes a single file; onValidityChange fires true only when both required slots are populated. If @testing-library/react is not yet installed, install it as a dev dependency. Step 7: Run `npx vitest run tests/components/` and `npx tsc --noEmit`. Then start `npm run dev` in the background, open http://localhost:3000/estimate/new in the user's browser context (just confirm the server boots without error — the user will do the actual UI walkthrough). Report the dev server status. Constraints: only RFP mode uses typed slots; RFI mode UI is unchanged. File-size budget 400 LOC for UI components. No new runtime dependencies beyond @testing-library/react (dev only) if needed.
