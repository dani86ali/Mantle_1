Fix: BOMatic RFP mode produces zero priced rows because list prices are never loaded for SKUs in the uploaded BoQ workbook. The dispatcher loads prices from input.devices (which is empty for RFP) and never inspects the BoQ file. Step 1: Verify tenantId is correctly threaded. Read src/app/api/intake/route.ts around lines 60-135 and confirm runAndPersistPipeline receives tenantId. Read src/coordinator/intake-to-pipeline-input.ts and confirm PipelineInput.tenantId is set from the parameter. Read src/coordinator/pipeline-engine-dispatcher.ts lines 53-95 (runE2Stage) and trace: input.tenantId gates loadListPrices, but collectCiscoSkus(resolvedDevices) returns [] for RFP because resolvedDevices is empty when no explicit devices are supplied and E5 has not run (which is the RFP case per router.ts). Step 2: Plan the fix. For RFP mode where boqFilePath is set, the dispatcher must extract SKUs from the BoQ file and feed them to loadListPrices in addition to any from resolvedDevices. Read src/engines/e2/boq-detector.ts to see detectBoQType. Read the type-A through type-E parsers in src/engines/e2/parsers/ to find which one yields a SKU list. The parsers return BoQLineItem objects with a model or partNumber field. Step 3: Create a helper extractBoqSkus(filePath: string): Promise<string[]> in src/coordinator/pipeline-e2-pricing.ts. It detects the BoQ type, parses the file using the matching parser, and returns a deduped list of non-empty SKU strings. If detection fails or parsing throws, return [] (graceful, do not throw). Step 4: In src/coordinator/pipeline-engine-dispatcher.ts runE2Stage, after boqFilePath is resolved (line 78) and before the listPrices block (line 82), when input.mode === 'rfp' and boqFilePath is set, call extractBoqSkus(boqFilePath) and merge those SKUs with collectCiscoSkus(resolvedDevices) into the SKU list passed to loadListPrices. Log via logEvent the count of SKUs loaded from the BoQ file. Step 5: Write a vitest test in tests/coordinator/pipeline-engine-dispatcher.test.ts (create if missing). Mock the catalog adapter so getItems returns deterministic prices for two known Cisco SKUs. Construct a fixture E1Output with a fileClassifications entry pointing at a small XLSX BoQ at tests/fixtures/sample-boq.xlsx (create the fixture using exceljs if a writer is available, or reuse an existing test fixture). Run runE2Stage with mode='rfp' and tenantId set, assert that listPrices passed to E2 buildE2Input contains entries for the two SKUs. Step 6: Run `npx vitest run tests/coordinator/` and `npx tsc --noEmit`. Show full output. Constraints: engine directories never import each other; the new helper lives in coordinator/. Public function signatures must be typed (no any). File-size budget 200 LOC for coordinator files.
