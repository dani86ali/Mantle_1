Fix: src/coordinator/pipeline-engine-dispatcher.ts lines 97-114 runE3Stage silently logs a warning and returns undefined when E2 output or pricingConfig is missing, leaving the operator with no visible indication that the proposal was skipped. Make the skip visible to the UI. Step 1: Read src/coordinator/pipeline-engine-dispatcher.ts runE3Stage and confirm the skip path (the `if (!e2 || !input.pricingConfig)` branch). Read src/coordinator/types.ts to find ArtifactRegistry['e3'] structure. Read src/coordinator/pipeline-e3.ts toE3Artifacts to see what fields currently populate the e3 artifact. Step 2: Extend ArtifactRegistry['e3'] in src/coordinator/types.ts with an optional `skipReason?: string` field. Update any toE3Artifacts and parseE3 paths to preserve this field across persistence. Step 3: In runE3Stage (pipeline-engine-dispatcher.ts), when !e2 || !input.pricingConfig, replace the current logEvent+return-undefined with: set state.artifacts.e3 = { skipReason: <human-readable reason> }, keep the existing logEvent warning, and still return undefined for out.e3Output. The reason string must name which input was missing — examples: "E3 skipped: E2 output not available (BoM stage did not produce a result)" or "E3 skipped: pricingConfig missing — set partner discount, deal-reg discount, profit margin, and VAT in the intake form before re-running". Step 4: Find the UI page that displays pipeline results for an intake. Grep for `artifacts.e3` or `e3Output` references in src/app/ and locate the proposal/estimate detail page (likely src/app/estimate/[id]/ or src/app/dashboard/). When e3.skipReason is set and no proposal output exists, render a warning banner above the BoM display showing the skipReason. Use existing styling — a bordered div with an icon and yellow/amber background is fine; do not add a new component library. Step 5: Add a vitest test in tests/coordinator/pipeline-engine-dispatcher.test.ts: invoking runE3Stage without pricingConfig results in state.artifacts.e3.skipReason being set to a non-empty string, NOT undefined. A second test asserts that when both E2 output and pricingConfig are present, skipReason is undefined and the normal E3 path runs (mock runE3 to return a stub). Step 6: Run `npx vitest run tests/coordinator/` and `npx tsc --noEmit`. Show output. Constraints: do not change the engine itself — only the dispatcher and artifact shape. Keep the skip non-fatal: pipeline must still complete successfully so the operator can read the BoM even when proposal is skipped. File-size budget 200 LOC for coordinator/.
