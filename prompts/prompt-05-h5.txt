Fix: HLD section 10 (Migration) and LLD sections 16 (Cable Schedule) and 17 (Rack Elevations) contain literal stub text like "populated from generateCableSchedule() output" in the customer-facing DOCX. The actual data is computed by the generators but never formatted into the narrative sections.

Step 1: Read these files to understand the current structure:
Read src/engines/e5/hld-deterministic-sections.ts and find migrationPlaceholderSection() around lines 127-133.
Read src/engines/e5/lld-deterministic-sections.ts and find cable schedule and rack elevation stubs around lines 119-127.
Read src/engines/e5/hld-narrative-generator.ts to see where migrationPlaceholderSection is called and how sections are assembled.
Read src/engines/e5/lld-narrative-generator.ts to see where cable and rack placeholders are called.
Read src/engines/e5/migration-selector.ts for the MigrationApproach type structure (phases, rollback, risks).
Read src/engines/e5/cable-schedule-generator.ts for the CableScheduleEntry type structure.
Read src/engines/e5/rack-elevation-generator.ts for the RackElevation type structure.
Read src/engines/e5/e5-phase1.ts to see where migrationApproach is computed.
Read src/engines/e5/e5-phase2.ts to see where cableSchedule and rackElevation are computed.

Step 2: Replace migrationPlaceholderSection in hld-deterministic-sections.ts.
Change from migrationPlaceholderSection() to migrationSection(approach) that takes the MigrationApproach type.
Format the migration phases as a numbered list with phase name, description, duration, and rollback plan per phase. List key risks and mitigations.
If approach is undefined or empty, return "Migration approach to be determined during detailed design." Keep the same section number and title.

Step 3: Replace cable schedule stub in lld-deterministic-sections.ts.
Change from cableSchedulePlaceholder() to cableScheduleSection(entries) that takes CableScheduleEntry array.
Format as a markdown table with columns: From Device, From Port, To Device, To Port, Cable Type, Length.
If entries is empty, return "Cable schedule to be populated during site survey." Keep the same section number and title.

Step 4: Replace rack elevation stub in lld-deterministic-sections.ts.
Change from rackElevationsPlaceholder() to rackElevationsSection(racks) that takes RackElevation array.
Format each rack as a sub-heading with a table showing U Position, Device, Description.
If racks is empty, return "Rack elevations to be populated during site survey." Keep the same section number and title.

Step 5: Thread the real data through the narrative generators and phase functions.
In hld-narrative-generator.ts: update the call to use migrationSection(migrationApproach). Add migrationApproach as a parameter if not already present.
In lld-narrative-generator.ts: update calls to use cableScheduleSection(cableSchedule) and rackElevationsSection(rackElevations). Add parameters if needed.
In e5-phase1.ts: pass the migrationApproach result into the HLD narrative generator.
In e5-phase2.ts: pass cableSchedule and rackElevation results into the LLD narrative generator.

Step 6: Update any tests that assert on the old stub text.
Run: grep -rn "populated from\|PlaceholderSection\|placeholderSection" tests/engines/e5/ --include="*.ts" | head -20
Update matching tests.

Constraints: Do NOT change section IDs or section counts. Do NOT modify the generator functions themselves. Only change how their output is formatted into narrative sections. Keep all files under 200 lines. Graceful fallback when data is missing.
Run: npx vitest run tests/engines/e5/ 2>&1 | Select-Object -Last 20
Run: npx tsc --noEmit 2>&1 | Select-Object -Last 10
Show me test and tsc output.
