Fix: E2Artifacts does not persist validationResults (the per-rule detail array). In-process runs work because E3 reads it directly from E2Output. But re-runs that load from DB lose the per-rule validation detail. This is the same pattern as the bom persistence fix already applied. Step 1: Read src/coordinator/pipeline-e2.ts and find toE2Artifacts. It stores validationWarnings and validationStatus but NOT validationResults. Read src/coordinator/types.ts to see E2Artifacts fields. Read src/engines/e2/orchestrator.ts to see what validationResults looks like in E2Output (it is a ValidationResult array). Step 2: Add validationResults as an optional string field to E2Artifacts in types.ts. It will hold JSON-stringified ValidationResult array, same pattern as the bom field. Step 3: In toE2Artifacts in pipeline-e2.ts, add: if out.validationResults and out.validationResults.length > 0, set artifacts.validationResults to JSON.stringify(out.validationResults). Step 4: Add a helper parseValidationResultsFromArtifacts that takes e2 artifacts and returns ValidationResult array. If missing or invalid JSON, return empty array. Export it. Step 5: Check if any re-run path or proposal route needs to use this helper. Read src/app/api/estimates/[id]/proposal/route.ts and src/app/api/estimates/[id]/design/_rerun-e2e3.ts. If they reconstruct E3 input from stored artifacts, they should use the helper for validationResults. Step 6: Run npx vitest run tests/coordinator/ and npx tsc --noEmit. Show output.
