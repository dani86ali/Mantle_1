Fix: E1 produces a full compliance matrix with per-requirement rows but the coordinator only forwards summary stats to E3. The proposal compliance section is a paragraph of counts instead of the detailed matrix table evaluators expect. Step 1: Read src/coordinator/pipeline-e3.ts and find mapE1 around line 61-88. Find where complianceMatrix is mapped and confirm it only passes stats (total, compliant, partial, nonCompliant counts). Read src/coordinator/pipeline-e1.ts to see what E1Artifacts stores for the compliance matrix. The matrix is stored as a JSON string in e1.complianceMatrix. Read src/engines/e1/compliance-matrix.ts to understand the row structure (each row has fields like requirement, controlTopic, framework, status, evidence or similar). Step 2: Read the E3 input types to see what field would hold the matrix rows. Check src/engines/e3/orchestrator-types.ts or wherever E3Input is defined. If there is no field for matrix rows, add one: complianceMatrixRows as an optional array of objects matching the row structure from E1. Step 3: In mapE1 in pipeline-e3.ts, parse the JSON-stringified complianceMatrix from E1Artifacts and forward the rows to E3 alongside the existing stats. If the parse fails, pass empty array and log a warning. Step 4: Read src/engines/e3/section-generators.ts and find the compliance section generator. Update it to render the matrix rows as a markdown table in the proposal section content when rows are available. Use columns: Requirement, Framework, Control, Status. Fall back to the current stats-only paragraph when rows are empty. Step 5: Run npx vitest run tests/engines/e3/ tests/coordinator/ and npx tsc --noEmit. Show output.
