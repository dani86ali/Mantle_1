Fix: E4 phase1 only writes the questionnaire artifact and never writes requirementsBaseline. When the pipeline continues to E5, E5 receives an empty object and designs against nothing.

Step 1: Read these files:
Read src/engines/e4/orchestrator.ts. Phase1 at line 79-90 returns artifacts with only questionnaire and no baseline.
Read src/engines/e4/phase1.ts for what Phase1Result contains and what data is available after phase1 runs (project type, emphasis matrix, sections).
Read src/engines/e4/orchestrator-types.ts for the E4InputData type (has siteCount, portCount, userCount, bandwidthGbps, vendor, boolean flags).
Read src/engines/e4/emphasis-matrix.ts for what the emphasis matrix produces.
Read src/engines/e4/requirements-baseline-builder.ts to see how phase2 builds the full baseline and what structure it uses.
Read src/coordinator/pipeline-e5.ts around line 64 where parseBaseline reads requirementsBaseline to understand what shape E5 expects.

Step 2: Create src/engines/e4/preliminary-baseline.ts.
Export a function buildPreliminaryBaseline that takes projectType (string), emphasisMatrix (the emphasis matrix result), and inputData (E4InputData), and returns a Record<string, unknown>.

This function should:
Use the project type and emphasis weights to infer which requirement categories are relevant (network, security, wireless, dc, voip).
Use inputData fields (siteCount, portCount, userCount, bandwidthGbps, vendor, hasWireless, hasVoice, hasDC, hasOT, hasGPON, hasHPC, isGreenfield) to populate sizing hints.
Structure the output to match what parseBaseline in pipeline-e5.ts expects so E5 can consume it.
Mark every entry with source: "preliminary" so downstream code knows this is inferred, not from customer responses.
Keep under 150 lines.

Step 3: Update E4 orchestrator phase1 return.
In src/engines/e4/orchestrator.ts line 84, change the phase1 artifacts from:
  artifacts: { questionnaire: r.questionnaireMd }
to include the preliminary baseline:
  artifacts: { questionnaire: r.questionnaireMd, requirementsBaseline: JSON.stringify(preliminaryBaseline) }

Build the preliminary baseline using phase1 result data (project type from r.projectType, emphasis from r.emphasisMatrix or similar) plus the input data. Read the Phase1Result type to find the correct field names.

Step 4: Verify E5 can consume it.
Read src/coordinator/pipeline-e5.ts parseBaseline to confirm the preliminary baseline structure is compatible. If parseBaseline expects a specific shape like a JSON string or a structured object, match that format.

Constraints: Do NOT change phase2 or the full baseline builder. The preliminary baseline is a subset that should never override a real phase2 baseline when one exists. Keep new file under 150 lines.
Run: npx vitest run tests/engines/e4/ tests/engines/e5/ 2>&1 | Select-Object -Last 20
Run: npx tsc --noEmit 2>&1 | Select-Object -Last 10
Show me test and tsc output.
