Fix: The central download route /api/estimates/[id]/download only supports bom, compliance, proposal, and financial artifacts. HLD, LLD, diagram, filledBoq, and componentList are missing.

Problem: src/app/api/estimates/[id]/download/route.ts only handles artifact types bom, compliance, proposal, financial. But the pipeline produces additional downloadable artifacts: HLD DOCX from E5 design state, LLD DOCX from E5 design state, Diagram XML draw.io from E5, Filled client BoQ XLSX from E2 artifacts, and Component list JSON from E5.

Step 1: Read these files first.
Read src/app/api/estimates/[id]/download/route.ts for the current download handler and existing patterns.
Read src/app/api/estimates/[id]/design/documents/route.ts for how it reads E5 state and streams docs.
Read src/app/api/estimates/[id]/_e5-state.ts for loadE5State which has HLD/LLD paths.
Read src/coordinator/types.ts for ArtifactRegistry showing e2.filledClientBoq field.

Step 2: Add new artifact types to the download route. Add switch cases for these artifact query param values:
'hld' should loadE5State and stream hldDocxPath file as application/vnd.openxmlformats-officedocument.wordprocessingml.document.
'lld' should loadE5State and stream lldDocxPath file as the same MIME type.
'diagram' should loadE5State and return diagramXml string as application/xml.
'filled_boq' should loadArtifacts and stream e2.filledClientBoq path as application/vnd.openxmlformats-officedocument.spreadsheetml.sheet.
'component_list' should loadE5State and return componentList as application/json.
Use the same file-streaming pattern already in the route with readFile and NextResponse with content-type and content-disposition headers.
If the route exceeds 200 lines, extract a helper file like _download-helpers.ts for the streaming logic.

Step 3: Update the export page. Read src/app/estimates/[id]/export/export-components.tsx. Add download buttons for the new artifact types. Only show each button when the artifact exists.

Constraints: Reuse existing file streaming patterns from the download route. Do NOT create new API routes. If route exceeds 200 lines, extract helpers into a sibling file. Tenant isolation is already handled by the existing resolve logic.
Run: npx tsc --noEmit 2>&1 | Select-Object -Last 10
Show me tsc output and list all files changed or created.
