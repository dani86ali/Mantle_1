Fix: src/app/customers/page.tsx uses hardcoded mock customer data instead of real data from the database. Step 1: Read src/app/customers/page.tsx to understand the current mock data structure and what fields are displayed. Step 2: Read src/lib/db/schema.ts for the intakes table which has customerName, region, country, domain, createdAt. Customers can be derived from unique customerName values in the intakes table. Step 3: Create or update an API route at src/app/api/customers/route.ts. The GET handler should call requireAuth for session, then query the intakes table grouped by customerName where tenantId equals session.tenantId. Return for each unique customer: name, region, country, domain, estimateCount as COUNT of intakes for that customer, lastActivity as MAX createdAt. Step 4: Update src/app/customers/page.tsx to fetch from /api/customers on mount using useEffect instead of using hardcoded data. Keep the existing UI layout and components as much as possible. Map the API response to the display format. Show an empty state with a message like No customers yet when there are no results. Step 5: Remove all hardcoded mock customer arrays from the page component. Step 6: Try to keep the page under 300 lines. If removing mock data reduces it significantly, that is good. Step 7: Run npx tsc --noEmit. Show output and list files changed.
