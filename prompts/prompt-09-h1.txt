Fix: RFI pipeline runs E4 then E5 then E2 then E3 synchronously without pausing for human input. The questionnaire is generated but E5 immediately runs against a preliminary baseline. The pipeline must pause after E4 phase1 and resume only after the user submits responses and E4 phase2 completes.

Step 1: Read these files thoroughly:
Read src/coordinator/pipeline-state.ts for ENGINE_CHECKPOINTS with e4:[] and e5:[], and the runCheckpoint function.
Read src/coordinator/pipeline.ts for the main runPipeline loop and how engines are called sequentially.
Read src/coordinator/types.ts for PipelineState type, IntakeMode, CheckpointStatus.
Read src/app/api/intake/route.ts where runPipeline is called as fire-and-forget.
Read src/app/api/estimates/[id]/responses/route.ts where the user submits RFI responses.
Read src/engines/e4/orchestrator.ts for runE4, isPhase2, phase routing.
Read src/coordinator/pipeline-e5.ts for buildE5Input where it reads E4 baseline.
Read src/lib/db/pipeline-store.ts for savePipelineState and loadPipelineStateByIntake.

Step 2: Add checkpoint definitions to pipeline-state.ts.
Change e4 and e5 from empty arrays to:
e4 gets two entries: id 'e4-questionnaire' with label 'Questionnaire review', and id 'e4-baseline' with label 'Requirements baseline review'.
e5 gets three entries: id 'e5-design-approach' with label 'Design approach review', id 'e5-hld' with label 'HLD review', id 'e5-lld' with label 'LLD review'.

Step 3: Modify the pipeline loop in pipeline.ts to support pausing for RFI mode.
In the engine loop, after E4 runs and its checkpoint is evaluated:
If mode is 'rfi' and the engine is 'e4' and E4 ran phase1 only (check if artifacts has questionnaire but the phase was phase1):
Set a status field on the pipeline state to 'paused_at_checkpoint'.
Set currentEngine to 'e4' to mark where we paused.
Save the pipeline state to DB via savePipelineState.
Return early from runPipeline. Do NOT continue to E5.
Log: "RFI pipeline paused at e4-questionnaire, waiting for client responses".

If PipelineState does not have a status field, add one as an optional string field in the types.

For non-RFI modes, continue normally. The existing checkpoint auto-approve behavior for e1, e2, e3 must NOT change.

Step 4: Create src/coordinator/pipeline-resume.ts with a function resumePipelineFromCheckpoint.
Export async function resumePipelineFromCheckpoint(intakeId: string, tenantId: string): Promise<void>
This function should:
Load pipeline state via loadPipelineStateByIntake.
Verify state.status is 'paused_at_checkpoint' and state.mode is 'rfi'.
Verify state.artifacts.e4.requirementsBaseline exists and is not empty.
Set state.status to 'running'.
Run the remaining engines starting from E5: call runE5 with buildE5Input, then E2, then E3, using the same logic and helpers as the main pipeline loop.
On success set state.status to 'completed'. On failure set state.status to 'failed'.
Save state after each engine completes.
Wrap everything in try/catch. Never throw. Log errors.

Step 5: Wire the responses route to trigger resume.
Read src/app/api/estimates/[id]/responses/route.ts. After the user submits responses and E4 phase2 runs successfully and the baseline is saved to artifacts:
Call void resumePipelineFromCheckpoint(intakeId, session.tenantId) as fire-and-forget.
This chains E4 phase2 done into E5 runs with real baseline into E2 prices into E3 proposes.

Step 6: Verify existing modes are unaffected.
RFP mode has e1 and e2 checkpoints that currently auto-approve with no callback. Adding e4 and e5 checkpoint definitions must NOT change this behavior. Verify that runCheckpoint still returns 'approved' when no callback is provided.
Quick BoM mode has no E4 or E5 so it is unaffected.

Constraints: The pause is ONLY for RFI mode at the E4 phase1 boundary. Do NOT modify E4 or E5 engine internals. Do NOT change runCheckpoint semantics for e1 e2 e3. The resume function should reuse existing engine-running helpers, not duplicate them. Keep all files under 200 lines.
Run: npx vitest run tests/coordinator/ tests/api/ 2>&1 | Select-Object -Last 30
Run: npx tsc --noEmit 2>&1 | Select-Object -Last 10
Show me test and tsc output.
